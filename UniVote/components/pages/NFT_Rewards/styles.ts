import { StyleSheet, Dimensions } from "react-native";
import { length_factor } from "../../shared/styles/styles";

export const styles = StyleSheet.create({
 
    details_wrapper:{
        width:'100%',
        padding: 17* length_factor,
        display: 'flex',
        justifyContent:'center',
        alignItems:'flex-start'
    },
});
