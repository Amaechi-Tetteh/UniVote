import { StyleSheet, Dimensions } from "react-native";
import { length_factor } from "../../shared/styles/styles";

export const styles = StyleSheet.create({

   image_container:{
       height:301*length_factor,
       width:'100%',
       backgroundColor:'grey'
   }


});
